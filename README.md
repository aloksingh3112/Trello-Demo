# Steps to run the following project

1. Unzip the file and open the folder in your desired command line window.
2. Install the latest version of [node.js](https://nodejs.org/en/) and npm.
3. Run `npm install` to download node_modules.
4. Open `localhost:3000` in your browser to see the webapp in action.
